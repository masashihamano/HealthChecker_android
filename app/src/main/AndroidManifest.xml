<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hamano.masashi.health_check">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".bmiActivity" />
        <activity android:name=".ScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Bmi_ResultActivity" />
        <activity android:name=".RohrerActivity" />
        <activity android:name=".RohrerResultActivity" />
        <activity android:name=".KaupActivity" />
        <activity android:name=".KaupResultActivity" />
        <activity android:name=".TallActivity" />
        <activity android:name=".TallResultActivity" />
        <activity android:name=".BloodActivity" />
        <activity android:name=".BloodResultActivity"></activity>
    </application>

</manifest>